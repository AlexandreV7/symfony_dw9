{% extends 'base.html.twig' %}

{% block title %}Edit Plat{% endblock %}

{% block body %}
    <h1>Edit Plat</h1>

    {{ include('plat_crud/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_plat_crud_index') }}">back to list</a>

    {{ include('plat_crud/_delete_form.html.twig') }}
{% endblock %}
